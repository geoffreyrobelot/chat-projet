{
  "name": "chat-server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "client": "cd chat-client && npm start",
    "install chat-client": "cd chat-client && npm install",
    "build": "cd chat-client && npm run build"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  }
}
